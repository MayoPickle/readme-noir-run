# Robert Yu Personal Homepage

A static personal homepage for Robert Yu, built with Next.js, Tailwind CSS, and shadcn/ui.

## Commands

```bash
npm run dev
npm run lint
npm run build
```

The site is configured for static export with `output: "export"`. Profile, skills, featured projects, and the project archive live in `src/lib/profile.ts`.
